#pragma once

#define ARRAY_SIZE 10

/**
 * @brief Sorts an array using the merge sort algorithm
 * 
 * @param arr Pointer to the array to sort (must point to ARRAY_SIZE elements)
 */
void mergeSort(int *arr);